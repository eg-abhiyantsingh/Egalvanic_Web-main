# Web v2.2 release check — one fix landed, one "Done" ticket is not done, one fix is half a fix

**Date:** 2026-09-14 (release day) · **Release:** Web v2.2 (Jira version 14156, 37 issues)
**Build under test:** `index-BTBN1KL5.js` — rebuilt since the 11 Sep sweep, so this is the release
candidate and not a stale bundle.

## The release at a glance

37 issues, all Medium priority, 4 Bugs + 33 Tasks. 33 Resolved (migrated), 3 To Do, 1 In Progress.
Eric Ehlert holds 36, Krunal lunagariya 1.

**Four of them are findings I raised and re-tested:**

| Ticket | Status | My last verdict | Today |
|---|---|---|---|
| ZP-4159 Facility Manager opens work orders for unassigned sites | In Progress | confirmed 11 Sep | **work-order half FIXED** |
| ZP-3834 failed Fill-from-Photos job stores the raw ECS error | To Do | confirmed 11 Sep | not retested today |
| ZP-3782 multiselect answers lose ticked options | To Do | confirmed still failing 9 Sep | not retested today |
| ZP-3783 review dialog ships option fields without their options | To Do | confirmed still failing 9 Sep | not retested today |

## 1. ZP-4159 — the named half is fixed. The rest of the family is not.

The ticket names the work-order **details** endpoints, and those are now closed:

| Call, Facility Manager, the exact work order in the ticket | 11 Sep | 14 Sep |
|---|---|---|
| `GET /api/ir_session/{id}/full` | 200 JSON, `account_name: "Test op"` | **200 text/html (refused)** |
| `GET /api/ir_session/{id}/team` | 200 JSON | **200 text/html** |
| `GET /api/ir_session/{id}/summary/v2` | 200 JSON | **200 text/html** |

**But the same seat, on the same unmapped site, still reads everything else.** FM is assigned 11 of the
tenant's sites; this site is not one of them:

| Call | Result today |
|---|---|
| `GET /api/nodes/sld/{id}` | **200 JSON — 292 assets** |
| `GET /api/lookup/issues/{id}` | **200 JSON — 80 issues** |
| `GET /api/lookup/v2/nodes/{id}` | 200 JSON — 50 rows |
| `GET /api/lookup/node-class-counts/{id}` | 200 JSON — 30 classes |
| `GET /api/sld/{id}/library-designations` | 200 JSON — 25 rows |
| Control: `GET /api/sld/{id}` (diagram) | **422 refused** |
| Control: `GET /api/connections/v2/sld/{id}` | **422 refused** |

This is the predicted outcome of fixing the endpoints a ticket names rather than the rule behind them.
**Recommendation: ZP-4159 should not close on the work-order fix alone.** The check belongs in one place
that every `sld_id`-scoped read passes through.

## 2. ZP-4145 is marked Done and is NOT fixed

*"Users Without Engineering Designations Entitlement Can Access Restricted Engineering Schedule Pages"* —
**Done**, Priority High.

Project Manager, `is_eg_admin: false`, 95 permissions, **does not hold**
`features.equipment_designations.view`. On today's release build:

| Engineering schedule page | Rows handed to this seat |
|---|---|
| Equipment Designations | **156** |
| OCPD Settings | **114** |
| Short-Circuit Ratings | **167** |
| Feeder Schedule | **40** |
| Transformer Schedule | **2** |

The page renders, titled "Equipment Designations", for an account without the entitlement.
Evidence: `docs/bug-evidence/2026-09-14-release-check/zp4145-done-but-still-open.png`

**Recommendation: reopen ZP-4145.** It is closed against a bug that reproduces on the build shipping today.

## 3. The two list leaks from 11 Sep are unchanged

Neither has a ticket, and neither is in this release:

- `GET /api/users/` from the Project Manager → **265 users, 43 belonging to 7 other companies**
- `GET /api/node_classes` → **637 rows, 527 belonging to 11 other companies**

Regression coverage exists: `mvn test -DsuiteXmlFile=suite-tenant-site-scope.xml`.

## Not changed in Jira

Nothing. ZP-4145 needs reopening and ZP-4159 needs its scope widened, but per the standing rule both are
raised here for the owner to decide rather than actioned. Note also that **ZP-4159's priority field reads
Medium while its own description says Severity High / Priority High** — the same field-versus-text mismatch
that ZP-4160 had.
