# Method-first, multi-service work orders (derived WO type + session_method_lines ledger)

**QA verdict — every step that can be driven from the web PASSES; two steps are iOS-only and one negative could not be exercised with the fixtures available. The ticket's "dev only, not yet promoted to QA" note is wrong for the web: the ledger endpoint persists per-line ids, every service carries a populated `wo_view`, and form instances carry `session_method_line_id` — the objects the three migrations create are all serving on QA (they were already serving on 7 Sept, so this is consistent with the migrations having run, not proof of *this* deploy). The session view is assembled from the services actually registered — adding a fourth service made a tab, a column and a registration-scoped ring appear live, and removing it took the tab and ring away — though the bundle shows the tab rule is driven by service *type*, not by the `tabs`/`hide_tabs` arrays. Forms mint exactly once, and Remove Service kept a submitted form twice over: a shared-scope Torque Record and, in a re-run to remove the confound, a node-scoped Cleaning fragment. The reported iOS casing bug is fixed on both server boundaries. No defects; 2 Low findings and one design question.**

**Ticket:** [ZP-3987](https://egalvanic.atlassian.net/browse/ZP-3987)
**Artifact:** https://claude.ai/code/artifact/18b40ab9-f75e-4820-9995-6a8b98b58830
**Tested:** 2026-09-08 · `acme.qa.egalvanic.ai` build **V1.36**, bundle **`index-CSsDpG3c.js`** · tenant acme · Super Admin seat · live UI clicks with request capture, plus direct calls on the two service boundaries to reproduce the iOS uppercase-UUID case.

> **Migration status (the ticket's own carry-forward caveat).** What the API shows: `services.wo_view` is returned fully populated for every service (`data_mask`, `readiness_mask`, `row_slot`, `tabs`, `hide_tabs`, `extra_columns`, `report_work_type`, `priority`); `session_method_lines` is readable and writable through `GET`/`POST /api/ir_session/{id}/method-lines`, which returns per-line ids that survive a remove-and-re-add; and a form instance carries `session_method_line_id` pointing at its method line (smline_a2's attribution column). All three objects were already serving on 7 Sept (bundle `index-BV5BwzBG.js`, ZP-4018 run), so their presence today is **consistent with** a1/a2/a3 having run and cannot distinguish a migrated column from application defaults, nor attribute them to this promotion. Only the deploy log or the database can. The frontend `src/sld/` OTA note is not observable from the browser.

---

## QA-review checklist — results

| # | Ticket step | Result |
|---|---|---|
| 1 | Create a work order and register multiple services on it; confirm web SessionDetail renders one tab and metric column per service plus a registration-scoped completion wheel | ✅ **PASS, with the mechanism stated as the code has it** — no work order was created in this run; the 3-service fixture is the ZP-4018 work order (its third service was attached the day before through the add-assets call, since that IR service is hidden from the wizard picker). It renders tabs Assets · SLD · Engineering · Issues · IR Photos · Attachments. **How that set is actually decided (bundle read after the adversarial pass):** Assets/Issues/Attachments always; Tasks never (hidden unconditionally); SLD + Engineering when a service of type AF is registered; Forms when a service has `row_slot:"forms"` or is of type PM Forms; IR Photos when any service's `tabs` names `ir_photos` or an IR/IR-Checklist service is registered. The `hide_tabs` arrays are computed into a set and never read, so "a show beats a hide" is true only in the sense that hides do nothing. Metric columns are one per distinct `data_mask` in priority order — `svc_mask_arcFlash` and `svc_mask_ir` — so three services yield two columns; the IR-Checklist check square is placed by a hard-coded rule (ir column + IR Checklist type), and only the per-node gate reads `extra_columns`. The wheel is registration-scoped: `arc_flash 5/10`, `asset_checks 4/5`, `ir_photos 1/1`, header = their sum (10/16 = 63 %). **Proven dynamically:** adding a PM Forms service put a `forms_status` column, a `Forms 4` tab and a ring `"Clean, Tighten, Torque": 0 of 1` on screen (wheel 63 % → 59 %, since a 0/1 ring can only lower it); removing it took the tab and the ring away and the wheel returned to 63 %. |
| 2 | Right-click an asset → Add Service (evaluated-forms pre-checked) and Manage Services; confirm forms mint exactly once (no double-mint) and that Remove Service takes only its unsubmitted, non-shared forms while submitted and shared-scope forms survive | ✅ **PASS** — the context menu carries **Add Service** and **Manage Services**. Add Service evaluates the service's rules against the asset and lists them under **EVALUATES TO** with **every method pre-checked**; applying minted 4 form instances from 2 methods. **No double-mint:** the identical request repeated returned `form_instances_created: 0` and echoed the same two line ids, with the ledger unchanged. **Removal scope, twice:** one of the four forms (Torque Record) was filled and submitted through the UI, then Remove Service returned `{"instances_removed":3,"lines_removed":2}` — the three drafts went, the submitted one stayed. Because the project's own notes call Torque Record a shared-scope form, that survival was confounded, so the cycle was **re-run**: re-adding the service minted 3 instances (the existing Torque Record was not minted again), a node-scoped **Cleaning fragment** was submitted through the UI, and Remove Service returned `{"instances_removed":2,"lines_removed":2}` leaving exactly the two submitted instances. "Submitted stays" holds independently of scope. (`by-session` reports `shared: false` on both survivors, so the shared-scope half of the step was not separately exercised.) |
| 3 | Send a service add and a service remove with uppercase client UUIDs; confirm the add applies and the remove is not silently no-opped (the `uuid::text` casing bug) | ✅ **PASS — fixed on both server boundaries** — add with an uppercase `node_id` and uppercase method ids → synchronous 200, 2 form instances created, 2 ledger lines written, registry grew to 5 services and a new ring appeared. Remove with an uppercase `node_id` and `service_id` → 200 **`{"instances_removed":2,"lines_removed":2}`** and the ledger, registry and ring all went back — not the `{0,0}` an unknown id produces. Transport facts: cookie-authenticated browser fetch, ids only in the JSON body (the path carries the lowercase session id), the web client applies no case normalisation, no `x-direct-write` header. So the SQL-level fix in these two handlers is proven; the iOS transport, its sync queue and the other write paths iOS uses (`line-checks`, `asset-checks`, `add-assets`) are not. |
| 4 | iOS offline: run Manage Services with no network, then sync; confirm the optimistic change replays and self-heals, and the Active Rooms default view + multi-service k/n chooser behave | ⚪ **Not web-testable** — native app (#517). If the iOS sync queue targets the same two handlers as the web (not verified), step 3 and the idempotent add cover their server side; a repeated remove of an already-removed known service was not run. |
| 5 | Confirm a single-service node shows its own metric and a direct tap action, while a multi-service node shows a k/n bubble and a chooser | ⚪ **Not web-testable** — the k/n bubble, chooser and direct tap are iOS presentation. Side note from the web grid, which shares the model: per-node cell gating is a registry check (`by_node`), and a cell for a service the node is not registered for renders completely empty with no control, while a registered one renders its metric plus its check square. |
| 6 | Confirm iOS check-offs persist onto the 1.60 model (`mapping_node_session.is_checked` via nodeChecks) and that no ghost grouped-task rows appear | ⚪ **Not web-testable** — native app. The web half of the same check-off contract (`PUT …/line-checks`, one unit per node/service, `status → executed`) was verified in this repo's ZP-4018 and PM-programs runs (no `line-checks` call was made in this run); no grouped-task rows appear on web because the Tasks tab is hidden unconditionally in this build. |
| 7 | Negative: a pre-ledger WO still resolves its type from legacy `work_type_id`; a plan released with frozen (pre-reseed) method ids skips and logs instead of returning a 500 | ⚠️ **NOT EXERCISED for the legacy-type half; PASS on "no 500" for unknown ids** — the two August work orders used as "pre-ledger" fixtures both have `work_type_id: null` in `GET /api/ir_session/{id}/full` (one was created by this repo's own WTE_014 test with work type General), so their bare three-tab view is the no-service view, and legacy type resolution was not tested. The field is exposed after all — `/full` carries `session.work_type_id`, and on the ledger-era work orders it holds the first registered service's id (AF on the 3-service order, De-Energized Visual Inspection on the PM Forms order). On method ids: a never-existed UUID on the add path → 400 naming it; the same with one valid id → 400, nothing written; the PM-plan apply path → 400 naming the line. No 500 anywhere, but these were unknown ids, not genuinely frozen pre-reseed rows, and the plan-release path is not reachable from the web. |
| 8 | Migration: verify the Run Database Migrations step succeeds and that `session_method_lines` and `services.wo_view` exist | ⚪ **Objects present; step success not observable** — the three objects the migrations create are serving (details in the callout above), and were already serving the day before. Whether the deploy's migration step succeeded, and whether indexes, constraints or backfills came with it, is not visible from the product. |

---

## Findings

### FINDING 1 — withdrawn after the adversarial pass (was: a metric column outlives its last registration)
After the last node was de-registered from *Clean, Tighten, Torque*, the Forms tab and the completion ring disappeared but a `forms_status` column stayed, surviving a reload. The bundle explains it: when no PM Forms service is registered, the grid falls back to the **legacy EG-Forms column**, shown whenever the work order has any form instances at all — and the surviving submitted form keeps that count above zero. The column has a value and a click target for CB-3836 (the green tick in the after-removal screenshots). It is the legacy column doing its job, not a stale derived column; folded into the design question below.

### FINDING 2 (Low, informational) — unknown method ids are refused whole on the user-initiated paths; the frozen-id plan release was unreachable
`POST …/method-lines` with one never-existed and one valid method id returns **400 `unknown or inaccessible method(s): …`** and adds nothing; the PM-plan apply path answers 400 `lines[0]: implementation methods do not belong to that service for this class`. For a user-initiated add, refusing the whole batch is arguably correct. The ticket's "skips and logs" expectation is about the plan-*release* path with genuinely frozen (superseded) rows, which is not reachable from the web UI and may take a different branch than "unknown". Recorded as a note on what was and was not reachable, not as a defect.

### FINDING 3 (Low, design question) — a service whose form was submitted drops out of the derived type once its last line is removed
The submitted forms correctly survive Remove Service and remain reachable — through **More → Forms** and through the asset's Forms cell in the grid. But once the service has no registered node it also leaves the derived set: the Forms tab goes, the ring goes, and the service is no longer among the work order's report types, even though work under it was performed and signed off. Whether a service with a submitted form should be removable from the derived type at all — or should stay registered in a completed state — is the question worth putting to the developers; the current behaviour is consistent with the dialog's promise but discards evidence of performed work from the type.

---

## Test data — direct links (QA)
- Multi-service work order used throughout: https://acme.qa.egalvanic.ai/sessions/11b924b8-f16f-49da-98d8-10ca3f2549dd
- Its registry / ledger / completion: https://acme.qa.egalvanic.ai/api/ir_session/11b924b8-f16f-49da-98d8-10ca3f2549dd/service-registry · https://acme.qa.egalvanic.ai/api/ir_session/11b924b8-f16f-49da-98d8-10ca3f2549dd/method-lines · https://acme.qa.egalvanic.ai/api/ir_session/11b924b8-f16f-49da-98d8-10ca3f2549dd/service-completion
- Asset used for Add / Manage / Remove Service: CB-3836 `51841f5f-1460-4130-842a-867cbfb7581e`
- The submitted form that survived removal: https://acme.qa.egalvanic.ai/api/eg-form-instance/4745511d-a1a0-42d7-93bd-cec7fd543b64
- Pre-ledger work orders: https://acme.qa.egalvanic.ai/sessions/61317671-017a-4d26-8b0e-adb997dfdce3 · https://acme.qa.egalvanic.ai/sessions/e5179624-9f51-4411-b190-3738f49cbc9e
- Single-service PM Forms work order created earlier the same day: https://acme.qa.egalvanic.ai/sessions/b2c2657a-50a0-4d69-af07-6167a0c0d39f

Evidence: `docs/bug-evidence/zp-method-first-multi-service-wo/` — screenshots plus `api-captures.md` with every request and response quoted (the burned-in caption on `n8-…` misstates why the column remained; the file's caption note corrects it).

---

## Not covered / honest gaps
- **All of iOS #517** — offline Manage Services replay, the sync-queue target, the Active Rooms default view, the k/n bubble and chooser, and the nodeChecks reconciliation cherry-pick. Native app only.
- **Shared-scope forms surviving a removal** — "submitted survives" was shown for both a shared-style form (Torque Record) and a node-scoped fragment; the API reports `shared: false` on both, so "shared-scope survives" as a separate rule was not exercised.
- **Cross-path double-mint** — idempotency was shown by repeating the same add; registering a service already present via a different path (wizard preload, PM-plan apply) then Add Service was not run.
- **A repeated remove of the same, already-removed service** — not run; only an unknown service id was.
- **The frozen-method-id plan release** — the release path is not in the web UI; the two reachable write paths refuse never-existed ids with a 400, which says nothing about genuinely superseded rows.
- **Legacy type resolution on a pre-ledger work order** — both August fixtures have `work_type_id: null` (no-service work orders); a pre-ledger order with a real legacy type was not located.
- **The migration step's deploy log** and the `src/sld/` OTA republish — outside the product.
- **Roles other than Super Admin** — mandatory MFA on QA blocks fresh logins of the other seats.
- Test data added and left in place on the shared QA work order: one submitted "Torque Record" form on CB-3836 (labelled with QA-DEMO values).
