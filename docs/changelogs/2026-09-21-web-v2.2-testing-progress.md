# Web v2.2 — testing progress, read live (2026-09-21)

**Prompt:** "testing progress? for all this ticket" — Jira release report for ZP version 14156 (Web v2.2).

## What I did

1. Read the release live from Jira (`fixVersion = 14156`): **40 issues**, version **not released**,
   release date **2026-09-25** — four days out. (It was 37 issues when the 14 Sep run started.)
2. Cross-referenced every key against the repo's recorded verdicts
   (`docs/test-runs/2026-09-14-web-v2.2-ticket-by-ticket.md`,
   `docs/report-artifacts/2026-09-14-web-v2.2-ticket-verdicts.html`).
3. Checked the live QA bundle hash, and re-ran the one promotion blocker against the current build.
4. Published the progress board: <https://claude.ai/artifact/FD2esA3TgCg2cenoDEHP26>

## The count

| State | Count |
|---|---|
| Verdict recorded (14 Sep) | 12 |
| Defects QA filed into the release (ZP-4189, ZP-4212) | 2 |
| Not yet tested | 26 |
| **Total in Web v2.2** | **40** |

Of the 12 verdicts: 4 verified shipped, 5 confirmed defects, 3 blocked on QA data/environment.

## Two findings from today's re-check

**1 · ZP-4042 still blocks promotion.** `GET /api/reporting/history` returns 500 `internal_error` on the
current build, bare, with `limit=5`, and with a real `sld_id`. Fresh trace ids
`becd79c41eaf4085a95ba02b8f346bee` and `7dc46732a149417c829087b5a84f6f2b`. Positive control in the same
session: `GET /api/reporting/configs` → 200 JSON, so the reporting blueprint is healthy and only this route
faults — consistent with the code reaching QA while the `report_generations` table did not.

**2 · The build moved under every verdict.** The 14 Sep verdicts were taken on `index-DDSq5pRr.js`;
QA serves `index-BV-phiFE.js` today. Per the standing rule (never publish a finding inherited from an older
verdict without re-testing on the current bundle), each PASS needs a short re-confirmation before it counts as
release evidence. ZP-4042 got that treatment and still fails.

## Side note — ZP-4145 shipped broken

Tested 14 Sep, found **not fixed** while reading Done. It has since moved out of v2.2 into **Web v2.1**,
which released 11 Sep, and is still Done. A known-open access finding shipped under a closed ticket.

## Untested work, clustered for execution

| Cluster | Tickets |
|---|---|
| Highest priority, in dev | ZP-4292 |
| Maintenance & condition assessment | ZP-4039, 4040, 4041, 4112, 4113 |
| Eaton batch (COM, methods, trip units) | ZP-4084, 4086, 4109, 4110, 4111, 4127, 4128 |
| BCES engineering attributes | ZP-4167, 4170, 4171, 4174 |
| Site Walk Journal / AI pipeline | ZP-4067, 4068, 4153 |
| Platform, grids, reporting | ZP-4062, 4066, 4082, 4131, 4149, 4152 |
