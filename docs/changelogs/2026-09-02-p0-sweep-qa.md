# Quick P0 sweep of QA (owner ask: "any hotfix needed?")

**2026-09-02.** 16-endpoint core-API battery + Tasks-page check + latest CI triage.
**Verdict: NO new P0, no hotfix needed.** One 500 found = the KNOWN legacy task-list regression
(`GET /api/tasks/{sld}` → 500, fresh trace 5ddb449e6d7d43c5a244ba47c854e1ce, broken since ~Jul 28) —
users unaffected (UI uses POST /v2/tasks/list → 200, Tasks page renders 21 tasks). All other core
APIs 200, none >8s (slowest /eg-forms 2.8s). Suite 3's daily red = exactly the 4 documented
expected-red guards (task-list ×3 + planned_workorder_line tripwire), nothing new. Aging note: the
legacy task-list 500 is now ~5 weeks old — worth a nudge with the fresh trace id, but P1 not P0.
