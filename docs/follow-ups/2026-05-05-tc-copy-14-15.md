# Follow-up: TC_Copy_14 + TC_Copy_15 (target date 2026-05-05)

**Filed:** 2026-04-28 by abhiyant.singh@egalvanic.com (via Claude Code)
**Scheduled cron job:** `e0394cee` (one-shot, 2026-05-05 09:13 IST, session-only)
**Backup mechanism:** this file (in case the cron job loses session before firing)

## What to add

Two tests in `src/test/java/com/egalvanic/qa/testcase/CopyToCopyFromTestNG.java`:

### TC_Copy_14 — Explicit Serviceability enum snapshot

The fuzzy match in TC_Copy_12 (case-insensitive substring on "serviceab") lands on the
**"Explain why ... not fully serviceable"** textarea instead of the **"Select Serviceability"**
combobox, because both contain "serviceab".

Write a stricter capture that:
1. Scrolls the Edit drawer to ensure the Serviceability dropdown is in view
2. Snapshots that combobox's enum value separately (e.g., "Serviceable", "Out of Service", "Maintenance")
3. Runs Copy From with default toggles (Serviceability checked)
4. Asserts the AFTER value is one of the known valid enum values (or non-empty if BEFORE was non-empty)

### TC_Copy_15 — Toggle-OFF behavior

Inverse of TC_Copy_12. Tests the Step 2 toggle wiring directly:
1. Open Copy From, walk to Step 2 ("Select fields to copy")
2. **UNCHECK the Serviceability toggle** (leave Core Attributes + Asset Subtype checked)
3. Click Apply
4. Capture drawer state after
5. Assert: "Explain why ... not fully serviceable" field DID NOT change (Serviceability was unchecked)
6. Assert: a Core Attributes field (e.g., "Select voltage") DID change (that toggle stayed checked)

If both assertions hold, the toggle wiring is correct. If either fails, real product
behavior differs from the documented Step 2 toggle list.

## Helpers already in the file (don't re-create)

- `captureDrawerFieldValues()` — JS-based snapshot of drawer field values (label → value)
- `closeDrawerWithoutSaving()` — discard drawer state without DB write
- `dialogRadioRows()` / `dialogCheckboxRows()` — picker selection helpers
- `findCopyDialog()` — recognizes step 1 + step 2 titles
- `findDialogButton(dlg, ...labels)` — first enabled button matching priority list
- `findKeyMatching(map, ...fragments)` — fuzzy substring lookup
- `abbreviate(s, maxLen)` — log readability

## Verification commands

```bash
# Compile
mvn test-compile -q

# Single-test debug
mvn test -Dtest='CopyToCopyFromTestNG#testTC_Copy_14_...'
mvn test -Dtest='CopyToCopyFromTestNG#testTC_Copy_15_...'

# Full suite — should be 14 PASS / 0 skip / 0 FAIL
mvn test -Dtest='CopyToCopyFromTestNG'
```

## Constraints

- Branch policy: main only, never developer
- Auto-push after commit (per memory rule)
- Write a changelog at `docs/AI-FEATURES-CHANGELOG/2026-MM-DD-HH-MM-copytofrom-tc14-tc15.md`
- Honest skip > fake pass: if a test reveals divergent behavior, document it in the changelog rather than forcing a green result
- Falsifiable assertions: see TC_Copy_12 for the pattern (assertion encodes a hypothesis; failure means a real change to investigate, not test brittleness)

## Done criteria

- [ ] TC_Copy_14 PASSES with explicit Serviceability enum value asserted
- [ ] TC_Copy_15 PASSES with toggle-OFF behavior verified (or honest fail with diagnostic if behavior diverges)
- [ ] Full suite is 14/0/0
- [ ] Changelog written
- [ ] Committed + pushed to main
- [ ] Delete this follow-up file (work complete) OR mark it `[partial]` if blockers were hit
