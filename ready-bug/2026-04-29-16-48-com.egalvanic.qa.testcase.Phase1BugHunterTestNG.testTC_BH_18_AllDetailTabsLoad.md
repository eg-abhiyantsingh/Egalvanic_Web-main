# [Bug | MEDIUM] com.egalvanic.qa.testcase.Phase1BugHunterTestNG.testTC_BH_18_AllDetailTabsLoad

**Test:** `com.egalvanic.qa.testcase.Phase1BugHunterTestNG.testTC_BH_18_AllDetailTabsLoad`
**Date:** 2026-04-29T16:48:18.810162
**Classification:** REAL_BUG (50% confidence)
**Severity:** MEDIUM
**Page URL:** https://acme.qa.egalvanic.ai/assets/b6c7dcaf-5f60-492f-88bb-968e75d355af
**Page Title:** Egalvanic Platform
**Test Duration:** 45597 ms

## Steps to Reproduce

1. Login to `https://acme.qa.egalvanic.ai/assets` with the standard QA credentials
2. Navigate to https://acme.qa.egalvanic.ai/assets/b6c7dcaf-5f60-492f-88bb-968e75d355af
3. Reproduce the test scenario manually using the test source as reference
4. Observe the result described under "Actual Result"


## Actual Result

```
AssertionError: BUG: 7 of 7 detail tabs failed to load:
  - Core Attributes (only 0 visible elements — likely empty / errored)
  - OCP (only 0 visible elements — likely empty / errored)
  - Connections1 (only 0 visible elements — likely empty / errored)
  - Issues (only 0 visible elements — likely empty / errored)
  - Tasks (only 0 visible elements — likely empty / errored)
  - Photos (only 0 visible elements — likely empty / errored)
  - Attachments (only 0 visible elements — likely empty / errored) expected [
```

## Expected Result

Per the assertion: `BUG: 7 of 7 detail tabs failed to load:
  - Core Attributes (only 0 visible elements — likely empty / errored)
  - OCP (only 0 visible elements — likely empty / errored)
  - Connections1 (only 0 visible elements — likely empty / errored)
  - Issues (only 0 visible elements — likely empty / errored)
  - Tasks (only 0 visible elements — likely empty / errored)
  - Photos (only 0 visible elements — likely empty / errored)
  - Attachments (only 0 visible elements — likely empty / errored) expected [`

## Screenshot

Attached: `testTC_BH_18_AllDetailTabsLoad_FAIL.png` (also copied next to this report as `<this-file>-screenshot.png`)

## Likely Root Cause

Unclassified failure — needs manual investigation

## Suggested Fix

Review the stack trace and screenshot

---

_Auto-generated by `ReadyBugGenerator`. Review the above, edit if needed, then copy-paste into your Jira ticket. **Do not** push this file's contents directly to Jira via tooling — per project rule, Jira modifications need explicit per-ticket approval._
