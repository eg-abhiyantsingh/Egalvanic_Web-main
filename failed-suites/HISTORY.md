# Failed-test history

One line per day (newest first). Run a day's failures with
`mvn test -DsuiteXmlFile=failed-suites/failed-tests-<date>.xml`.

_No CI runs recorded yet — the first entry appears after the next Parallel Suite run._
